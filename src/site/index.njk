---
title: ElevenTail
layout: default
---
<div class="pr-32 md:pr-56 text-left mb-4 mt-4">
 {%- for post in collections.mainPages -%}
<div class="entryContainer btn-log">
  <div onclick="contentCollapse(event)" class="authorContainer text-tangerine btn-log">{{ post.data.author }} - {{ post.data.title }}</div>
  <div class="contentContainer text-quicksilver">{{ post.templateContent | safe }}</div>
</div>
{%- endfor -%}
</div>
